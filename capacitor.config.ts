import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'info.jpr0g.beatme',
  appName: 'beatMe',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
